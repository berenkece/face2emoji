# Face2Emoji - dogrudan bagimliliklar.
#
# Dolayli bagimliliklar (absl-py, matplotlib, opencv-contrib-python, pillow,
# protobuf, sounddevice, Werkzeug, Jinja2 vb.) flask ve mediapipe tarafindan
# otomatik kurulur; burada listelenmezler.
#
# Tum surumler sabitlenmistir: etkinlik oncesi "dun calisiyordu" surprizi
# yasanmasin diye yeni bir kurulum her zaman ayni surumleri getirir.

flask==3.1.3
opencv-python==5.0.0.93
numpy==2.5.3

# SURUM SABIT - YUKSELTMEYIN.
# mediapipe 1.0.1, macOS arm64'te FaceLandmarker olustururken native crash
# veriyor (DrishtiMetalHelper / Metal servisi bulunamiyor). Bu bir Python
# istisnasi degil, surecin tamami oluyor; try/except ile yakalanamaz.
# 0.10.35 ayni model dosyasiyla sorunsuz calisiyor.
# Ayrinti icin README > Bilinen sorunlar.
mediapipe==0.10.35

# Dahili Mac kamerasini dogru secmek icin (core/devices.py). macOS'ta
# Continuity Camera, iPhone'u OpenCV index 0'a itebiliyor; AVFoundation cihaz
# listesi okunarak dahili kameranin gercek index'i bulunuyor.
# Yalnizca macOS'ta kurulur, boylece Linux/Windows'ta kurulum patlamaz.
pyobjc-core==12.2.2 ; sys_platform == "darwin"
pyobjc-framework-AVFoundation==12.2.2 ; sys_platform == "darwin"
